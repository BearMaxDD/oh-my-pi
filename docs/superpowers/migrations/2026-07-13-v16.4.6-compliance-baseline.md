# OMP Advisor 合规完成门 — v16.4.6 基线迁移记录

> 日期：2026-07-13
> 目标基线：上游 `v16.4.6`（tag `20c0a2e4101d8507e7cbbaf547baa4f9f2340b73`）
> 归档来源：OMP `16.3.3` 重型树（commit `d1c66b4c412a86f3c6d1027c44f77c4b199e9c43`）

---

## 1. 归档锚点

原始重型实现树（基于 v16.3.3）已不可变归档。

| 属性 | 值 |
| --- | --- |
| 原始 HEAD 提交 | `d1c66b4c412a86f3c6d1027c44f77c4b199e9c43` |
| 描述 | `v16.3.3-33-gd1c66b4` |
| 归档标签 | `archive/strict-runtime-2026-07-13`（annotated tag） |
| 归档命令 | `git tag -a archive/strict-runtime-2026-07-13 -m "归档：严格路由与 PlanRun 重型实现"` |
| 推送状态 | 已推送至 origin |

该归档标签指向的树包含以下重型能力的完整实现：

- Codex PlanRun 执行引擎（`packages/coding-agent/src/codex-plan-run/**`）
- 严格角色路由与模型锁
- 角色模型批量分配
- 子代理模型回退控制
- 严格角色 E2E 测试套件

后续合规扩展开发**不迁移**上述能力到新基线。

---

## 2. v16.4.6 基线

| 属性 | 值 |
| --- | --- |
| 基线标签 | `v16.4.6` |
| 基线提交 | `20c0a2e4101d8507e7cbbaf547baa4f9f2340b73` |
| 新工作线分支 | `mima/omp-compliance-v16.4.6` |
| 工作树路径 | `../oh-my-pi-v16.4.6-compliance` |

工作树从 `v16.4.6` tag 创建，**不是**从远程引用 `upstream/v16.4.6`（该远程引用在此工作树中不可用——见下方修正说明）。

### 2.1 修正：`upstream/v16.4.6` 引用不可用

此工作树未添加 `upstream` 远程。基线验证时使用本地 tag `v16.4.6` 代替 `upstream/v16.4.6`。所有 diff 命令和引用检查均使用：

```bash
git diff --exit-code v16.4.6 -- <path>
# 而非：
git diff --exit-code upstream/v16.4.6 -- <path>
```

此工作树的唯一远程是 `origin`（`git@github.com:can1357/oh-my-pi.git`）。

---

## 3. RED/GREEN 断言证据

迁移前执行了双断言检查，确认已从正确的基线重新开始。

### RED 断言（应在 main checkout 上失败）

```bash
test "$(git rev-parse HEAD)" = "$(git rev-parse v16.4.6)"
```

**结果：** 在原始 `oh-my-pi` checkout 上预期失败。
- 原始 `oh-my-pi` HEAD：`d1c66b4c412a86f3c6d1027c44f77c4b199e9c43`
- v16.4.6 tag：`20c0a2e4101d8507e7cbbaf547baa4f9f2340b73`
- 结论：**PASS（预期不符）** — 原始 checkout 不等于 v16.4.6，RED 门正确阻止了未迁移的直接操作。

### GREEN 断言（应在 worktree 上通过）

```bash
test "$(git rev-parse HEAD)" = "$(git rev-parse v16.4.6)"
```

**结果：** 在新 worktree `oh-my-pi-v16.4.6-compliance` 上通过。
- worktree HEAD：`20c0a2e4101d8507e7cbbaf547baa4f9f2340b73`
- v16.4.6 tag：`20c0a2e4101d8507e7cbbaf547baa4f9f2340b73`
- 结论：**PASS** — 工作树已正确基于 v16.4.6 基线创建。

---

## 4. 迁移说明（基线验证命令）

在新 worktree 上运行以下命令以验证基线状态：

```bash
# 1. 安装依赖（使用 frozen lockfile 确保可重复性）
bun install --frozen-lockfile

# 2. 类型检查
bun --cwd=packages/coding-agent run check:types

# 3. Advisor 可见性测试
bun test packages/coding-agent/test/advisor/advisor-visibility.test.ts

# 4. 扩展加载测试套件
bun test packages/coding-agent/test/extensibility

# 5. TaskTool 子代理生成测试
bun test packages/coding-agent/test/task/task-spawn.test.ts
```

### 4.1 基线 diff 验证

```bash
# 验证 codex-plan-run 目录与 v16.4.6 完全一致（证明无核心代码变更）
git diff --exit-code v16.4.6 -- packages/coding-agent/src/codex-plan-run

# 验证模型路由文件与 v16.4.6 完全一致（证明无严格路由变更）
git diff --exit-code v16.4.6 -- packages/coding-agent/src/task/model-routing.ts
```

**预期：** 所有官方基线测试通过；两个 diff 返回空（退出码 0）。

---

## 5. 不迁移的能力

本计划明确不把以下四类重型实现能力带入 v16.4.6 新基线：

| # | 能力 | 相关路径/文件 | 替代方案 |
| --- | --- | --- | --- |
| 1 | **Codex PlanRun 引擎** | `packages/coding-agent/src/codex-plan-run/**` | 使用官方 TaskTool 子代理执行受管任务 |
| 2 | **严格角色路由** | `packages/coding-agent/src/task/` 中与严格角色分配、路由相关的文件 | 不使用角色分配层级；Advisor 只读审阅 |
| 3 | **模型锁** | `packages/coding-agent/src/task/model-routing.ts` 等模型锁定相关文件 | 使用 v16.4.6 原生模型回退机制 |
| 4 | **严格角色 E2E 测试** | `test/task/strict-role-*.test.ts` | 在合规独立包中以新方式编写测试 |

以上能力的运行时代码仅保留在归档标签 `archive/strict-runtime-2026-07-13` 中，供历史只读参考。**在新基线中不保留、不迁移、不替换它们的功能等价物。** 合规扩展使用官方 v16.4.6 扩展 API 和 Advisor 内置能力实现目标，不从归档树拷贝代码。

如需查阅这些能力的实现细节，请 checkout 归档 tag：

```bash
git checkout archive/strict-runtime-2026-07-13 -- packages/coding-agent/src/codex-plan-run/
```

---

## 6. 当前阶段状态

此阶段（任务 1）完成后：

- 工作树 `oh-my-pi-v16.4.6-compliance` 是干净的 `v16.4.6` 基线
- 不包含任何合规扩展代码
- 不包含上述四类不迁移能力
- 所有官方基线测试通过
- 文档和 README 修改已提交

后续任务（任务 2+）将在独立仓库 `/Users/mima1234/Code/super/omp-custom` 创建合规扩展包 `@bearmaxdd/omp-compliance`，**不在本工作树中修改运行时代码**。

